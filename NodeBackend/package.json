{
  "name": "firstapp",
  "description": "description",
  "authors": "author",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "build": "cd websitefront && npm install && npm run build"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.18.2",
    "nodemon": "^3.0.1"
  }
}
